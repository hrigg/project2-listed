<!DOCTYPE html>
<html lang="en">
	<%- include( 'partials/head.ejs', { page: 'House' } ) %> 
    <body>  
		<%- include( 'partials/nav.ejs' )  %>

        <section>
            <div>
                <h2 class="title">All Listings</h2>
            </div>
        </section>

        <main class="container">
            <div> 
                <h2>Chi</h2>
                <% for( let i = 0; i<chicagoHouse.length; i++){ %>
                    <% const firstHouse = chicagoHouse[i] %>
                    <a href="/house/<%=firstHouse._id%>"><%= firstHouse.name %></a>
                    <img src="<%=firstHouse.image%>" style="max-width: 200px;"> 
                <% } %>
            </div>
        </main>
    
	</body>
</html>